using System;
using UnityEngine;

namespace Gameplay.Modifiers
{
    public class ModifierTarget : MonoBehaviour
    {
        // public void AddModifier(IModifier modifier)
        // {
        //     if (modifier == null)
        //     {
        //         throw new ArgumentNullException(nameof(modifier), "Modifier cannot be null");
        //     }

        //     // Logic to add the modifier to this target
        //     Debug.Log($"Modifier {modifier.GetType().Name} added to target {gameObject.name}");
        // }
    }
}
